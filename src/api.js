// API FETCHING --------------------------------------------------------------------------------

export async function fetchShortUrl(longUrl) {
    const response = await fetch(//bitlyAPI goes here))
}